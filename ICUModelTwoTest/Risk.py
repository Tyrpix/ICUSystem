from enum import Enum


class Risk(Enum):
    LOW = 1
    HIGH = 2
# Create input data class and state class to determine states
# Need to handle state changes, state objects with methods that handle calculations
